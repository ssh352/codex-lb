# Tasks

- [x] Add `openspec/specs/dashboard-waste-pacing/{spec.md,context.md}`.
- [x] Add pure computation module `app/core/usage/waste_pacing.py` + unit tests.
- [x] Extend dashboard overview API response schema with `wastePacing`.
- [x] Compute and return pacing fields in `DashboardService.get_overview`.
- [x] Update dashboard frontend to render summary + per-account pacing.
- [x] Add/adjust integration test for `/api/dashboard/overview` to validate new contract.

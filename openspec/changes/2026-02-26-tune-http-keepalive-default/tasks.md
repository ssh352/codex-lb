# Tasks

- [x] Lower `http_client_keepalive_timeout_seconds` default to 10 seconds.
- [x] Add comments explaining the rationale and tuning tradeoffs.
- [ ] Validate in a proxy-backed deployment and adjust recommended values if needed.

